source 'https://rubygems.org'

# Use the library gemspec only (renamed to cv_printer)
gemspec name: 'cv_printer'

group :test do
  gem 'pdf-inspector'
  gem 'minitest'
end

group :benchmark do
  gem 'benchmark_driver', '0.15.17'
  # For generating benchmark reports with --output gruff
  gem 'benchmark_driver-output-gruff'
end